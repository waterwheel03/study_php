動画
http://schoo.jp/class/2410/room

window.onload：すべてのHTMLが読み込まれたときに実行

background-colorのようなやつでJavascriptだと"-"（ハイフン）は入力できないので
入力するときは「backgroundColor」のように大文字で指定する

addEventListener()

stopPropagation()
